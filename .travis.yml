language: node_js
sudo: false

cache:
  directories:
    - $HOME/.npm
    - $HOME/.yarn-cache

node_js:
  - "5"
  - "4"

install:
  - npm install yarn -g
  - yarn install

script:
- npm run lint
- npm run test

branches:
  only:
  - master
